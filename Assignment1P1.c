#include<stdio.h>
#include<conio.h>
#include<limits.h>

int main()
{
	printf("range of Short variable is from %d to %d\n", SHRT_MIN, SHRT_MAX);
	printf("Range of Char variable is from %ld to %ld\n", CHAR_MIN, CHAR_MAX);
	printf("Range of Integer variable is from %d to %d\n", INT_MIN, INT_MAX);
	printf("Range of Long variable is from %ld to %ld\n", LONG_MIN, LONG_MAX);
	printf("Range of Long Long variable is from %lld to %lld\n", LLONG_MIN, LLONG_MAX);
	
	

	return 0;
}